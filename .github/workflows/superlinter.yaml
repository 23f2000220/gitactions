name : Super-Linter

on : push
 
jobs:
 super-lint:
   name : Lint code base
   runs-on : ubuntu-latest
   steps:     
     - name: 23f2000220@ds.study.iitm.ac.in
       run: echo "Hello, world!"
     - name: Checkout code
       uses: actions/checkout@v2
     - name: Run Super_linter
       uses: github/super-linter@v3
       env: 
        DEFAULT_BRANCH: main
        GUTHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
